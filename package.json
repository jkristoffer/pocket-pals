{
  "name": "pocket-pals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "node --check src/domain.js && node --check src/storage.js && node --check src/app.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
